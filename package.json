{
    "name": "browserquest",
    "version": "1.0.0",
    "private": true,
    "type": "module",
    "scripts": {
        "lint": "npx eslint client/js/",
        "test": "node --test client/tests/unit/*.test.cjs",
        "check": "npm run lint && npm run test"
    }
}
